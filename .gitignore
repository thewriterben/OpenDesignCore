# Build
/target/
/build/
/dist/
bin/
obj/
*.user
*.o
*.so
*.dll
*.dylib
*.pyd

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Environment
.env
.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Machine state — the run ledger is append-only local state (ADR-0006), not history.
ledger.db

# Generated artifacts — outputs are reproducible, so they don't belong in history.
# Reference and example inputs are exempt below and tracked deliberately.
/out/
/artifacts/
*.stl
*.step
*.stp
*.3mf
*.gcode
*.vtk
*.vtu
*.msh
!tests/reference/**/*.step
!tests/reference/**/*.stl
!examples/**/*.step
